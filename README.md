# Galil Engineering Suite V5

## התחברות והרשאות

לאתר יש מסך התחברות (שם משתמש + סיסמה). למשתמשים יש שלושה תפקידים אפשריים:
- **צפייה בלבד** - לא רואה בכלל את אפשרויות העלאת הקבצים.
- **מורשה העלאה** - יכול להעלות Excel/PDF.
- **מנהל מערכת** - כמו מורשה העלאה, וגם יכול לנהל משתמשים (מסך "ניהול
  משתמשים": הוספה, מחיקה, שינוי תפקיד, טיפול בבקשות "שכחתי סיסמה").

ה-API שמטפל בזה (`/api/...`) הוא Vercel Serverless Function אחד (`api/index.js`,
עוטף אפליקציית Express מ-`lib/app.js`) שרץ **באותה פריסה בדיוק** כמו האתר עצמו -
אין צורך בשרת נפרד, בדומיין נפרד או בהגדרת VITE_API_URL.

### פריסה ב-Vercel

1. Upload / Import project, כרגיל. Build command: `npm run build`, Output: `dist`.
2. בפרויקט ב-Vercel: **Storage**, ושם **Create Database** או **Browse Marketplace**
   (התפריט משתנה מדי פעם - לפעמים זה "KV" ישירות, לפעמים רשימת שירותים כמו
   Upstash/Neon/וכו'). בכל מקרה, מחפשים **Upstash** ובוחרים סוג מסד נתונים
   **Redis** (יש שכבה חינמית). אחרי יצירת/חיבור מסד הנתונים - חשוב לוודא
   שהוא מחובר (Connected/Linked) לפרויקט הזה בדיוק. Vercel יוסיף אוטומטית
   משתני סביבה לפרויקט (בשם `KV_REST_API_URL`/`KV_REST_API_TOKEN` או
   `UPSTASH_REDIS_REST_URL`/`UPSTASH_REDIS_REST_TOKEN` - שני השמות נתמכים) -
   זה מה שמאפשר לרשימת המשתמשים ולהרשאות "לזכור" בין בקשה לבקשה (בניגוד
   לדיסק המקומי, ש-Vercel מוחק בכל קריאה).
3. **Settings → Environment Variables**, הוסיפו:
   - `JWT_SECRET` - מחרוזת אקראית וארוכה (ראו `.env.example` להסבר איך ליצור).
   - `JWT_EXPIRES_IN` - למשל `12h`.
   - `ADMIN_USERNAME` / `ADMIN_PASSWORD` - נוצרים אוטומטית כמשתמש הראשון
     בהפעלה הראשונה, אם עדיין אין אף משתמש. שנו את הסיסמה מיד אחרי ההתחברות
     הראשונה (ניתן אז גם להסיר את שני המשתנים האלה).
4. Redeploy (כל שינוי במשתני סביבה דורש דיפלוי מחדש כדי להיכנס לתוקף).

### הרצה מקומית

```
npm install
cp .env.example .env    # ומלאו JWT_SECRET משלכם
npm run dev:api         # מריץ את ה-API על פורט 4000
npm run dev              # בטרמינל נפרד: האתר עצמו, עם פרוקסי אוטומטי ל-/api
```

בלי משתני Redis (לא מוגדרים בברירת מחדל בפיתוח מקומי), ה-API נופל אוטומטית
לאחסון מקומי בקבצי JSON תחת `.local-data/` (לא נשמר בגיט) - כך שהכול עובד
מקומית בלי לחבר שום דבר, אבל **בפריסה בפועל ב-Vercel חובה לחבר Redis**
אחרת נתוני המשתמשים לא נשמרים בין בקשה לבקשה.

## פיצ'רים כלליים
- שתי לשוניות: מחירון / BOQ ומאגר ספקים
- עיצוב בהיר זהה לגרסה הקודמת
- העלאת Excel למחירון לפי כל הלשוניות: כל לשונית הופכת לדיסציפלינה
- דיסציפלינות ברירת מחדל: צנרת, חשמל, כיבוי אש, מיזוג אויר, הנדסה אזרחית, מכשור
- הוספת דיסציפלינה ידנית
- מחיקת דיסציפלינה נבחרת
- סל חישוב, שמירה, יצוא Excel, יצוא PDF
- מאגר ספקים עם העלאת Excel, חיפוש, דירוג, מחיקה וסיווג ידני
