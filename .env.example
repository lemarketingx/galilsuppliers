# Copy to .env (and set the same variable in your Vercel project settings).
# Address of the auth server (see the server/ folder) that powers login and
# upload permissions. Without this, the site works normally but nobody can
# log in, so upload buttons stay locked for everyone.
VITE_API_URL=http://localhost:4000
